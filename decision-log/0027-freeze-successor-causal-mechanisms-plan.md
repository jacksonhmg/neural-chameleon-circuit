# Decision 0027: Freeze the Successor Causal-Mechanisms Research Plan

- **Date:** 2026-08-08
- **Status:** Accepted

## Decision

Freeze `docs/causal-mechanisms-research-plan.md` as the controlling scientific plan for future work targeting the title *Causal Mechanisms of Activation-Monitor Evasion in a Neural Chameleon*.

Treat all evidence inspected through the accepted private manuscript as development evidence for the successor project. Prospectively freeze the 12 attention heads isolated by the earlier post-confirmatory K16 head/MLP falsification as the candidate writer population. Do not describe K12 as an untouched or originally confirmatory object.

The successor plan requires evidence for an acquired causal chain:

```text
pre-K12 monitored-concept representation
    -> predicted tokenwise K12 writer trajectory
    -> tokenwise layer-12 displacement
    -> layer-12 probe-margin and score changes
    -> operational monitor failure
```

It additionally requires a matched concept-conditioned contrast, predicted removal and insertion at the actual pre-`W_O` head hooks, restricted endogenous propagation from a frozen upstream mediator intervention set, concept-swap restoration, and genuinely fresh operational confirmation.

The working title remains conditional. It is authorized only if every final title gate in the plan passes on fresh evidence.

## Evidence reset

The accepted private manuscript and its sealed inputs remain valid historical artifacts of the completed first project. They do not provide fresh confirmation of the successor mechanism.

For the successor project:

- completed discovery, validation, safety, route, behavior, and manuscript analyses are development evidence;
- K16 remains the historical frozen intervention;
- K12 is the prospectively frozen candidate generated by later falsification;
- existing benign and safety concepts may be used freely for development; and
- final confirmation requires disjoint probe-training, phenomenon-qualification, threshold-calibration, and untouched evaluation data.

## Required evidence discipline

- Keep accounting, prediction, and causation explicitly separate.
- Keep total `T_c - N` and matched `T_c - T_d` contrasts separate.
- Do not present the observed pairwise delta identity as a mechanistic prediction.
- Enforce the predictor leakage contract in code and audit artifacts.
- Use fixed, magnitude-sensitive primary metrics for high-dimensional predictions.
- Calibrate each operational threshold on a separate normal-condition negative pool and keep it unchanged across all evaluation conditions.
- Report full-trajectory clamping as sufficiency, and claim endogenous propagation only when a restricted upstream intervention regenerates unpatched downstream writer states.

## Consequences

- The original `docs/neural-chameleon-evasion-research-plan.md` is historical for future-work purposes.
- The existing `EXPERIMENT_SPEC.md` remains authoritative for the completed first project. New mechanism phases require their own frozen implementation contracts.
- Decision 0025 and Decision 0026 continue to govern the existing private manuscript package; this decision does not retroactively upgrade its evidence or claims.
- The successor project must fall back to a narrower title if an acquisition, prediction, causal-control, propagation, restoration, or operational gate fails.
- No push, tag, release, submission, upload, external message, author contact, or new empirical result is authorized by this planning decision.
