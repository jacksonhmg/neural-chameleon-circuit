# Decision 0072: Reject the Proximal Response-State Controller

**Date:** 2026-08-10
**Status:** Accepted after the frozen Day 48 gate

## Decision

Apply the Day 48 gate without modification. The implementation audit passes and the scientific gate fails. Reject the layer-9-input response-token residual as a compact causal controller of the natural K12 trajectory.

Do not execute backward response-interface localization, component decomposition, factorization, mediation, or population validation on this failed candidate. Freeze and execute only the selected prompt-memory branch allowed by the Day 48 contract.

## Evidence

- sufficiency median concept K12 recovery: `0.04746`;
- necessity median concept K12 recovery: `0.02322`;
- advantages over the strongest controls: `0.03283` and `0.00825` versus required `0.25`;
- K12 direction correct in 13/13 sufficiency concepts but only 8/13 necessity concepts; and
- exact population K12 identity error: `0.0` after the batch-shape implementation repair.

Correct own-probe direction in 13/13 concepts in both directions is not enough to rescue the hypothesis because the primary natural K12 trajectory was almost absent.

## Boundary

The result falsifies only the tested proximal response-row controller. It does not show that K12 lacks upstream control. The prompt-side attention-memory interfaces remain eligible under a separately frozen contract. The Phase C named span remains a failed specificity comparator and may not be rebranded as the controller.
