# Decision 0092: Accept the Distributed QKV and Tail-Attention Mechanism

**Date:** 2026-08-12
**Status:** Applied frozen Day 58/59 dispositions

## Decision

Accept the Day 58 mechanical selections and the Day 59 held-out confirmation. The
condition-specific K12 state is constructed by a coordinated monitoring-prefix QKV
operation across the 12 selected heads. V-only and QK-only accounts are rejected.

Accept that exact K12 is not self-contained for the complete natural monitor
endpoint. A parallel, distributed contribution from the other 52 attention heads in
layers 9--12 is required for approximately 94% closure. Residual-9 and tail-MLP
candidates do not qualify bidirectionally.

Accept the exact-precursor classification `acquired`. All frozen precursor/Chameleon
effect-norm ratios are at most `0.0812`, substantially below the `0.50` ceiling, with
identical response tokens and masks.

## Consequences

- Upgrade the mechanistic claim from an unresolved K12 operation to an acquired,
  distributed QKV-mediated K12 plus parallel tail-attention mechanism.
- Preserve exact K12's role as the principal direct causal contributor, not a
  complete endpoint code.
- Do not privilege one head, layer, V content, QK routing, MLP, or proximal residual.
- Do not call the other-52-head object compact.
- Direct further work to the origin and factorization of the acquired QKV difference,
  with any smaller source-region or parameter claim requiring a new prospective test.
