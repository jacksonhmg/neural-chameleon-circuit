# Decision 0093: Accept a Source-Side K/V K12 Operation

**Date:** 2026-08-12
**Status:** Applied frozen Day 60 dispositions

## Decision

Accept `proper_subset_sufficient:kv`. Full-prefix donor keys and values reconstruct
the acquired K12 operation while response queries remain target-natural. KV passes
every prospectively frozen probe, K12, orthogonal-advantage, and concept-nearest gate
in both directions. No simpler factor subset passes.

Accept `distributed_across_prefix_regions`. The informative K/V source is divided
between named-concept and trigger/other tokens; neither passes the bidirectional,
cross-space dominance gate. Monitoring-language tokens contribute zero under this
intervention.

Retain the material Q-dependent completion effect. Q is not necessary for the frozen
sufficiency definition, but QKV improves upon KV and has a material three-way term.
Therefore do not relabel the full computation as strictly Q-independent.

## Consequences

- Replace the unresolved "coordinated QKV" account with the more specific claim that
  the principal sufficient operation is a joint source-side K/V rewrite read by
  target-natural response queries.
- Reject Q-only, K-only, V-only, QK, and QV as complete bidirectional accounts under
  the frozen gates.
- Do not infer parameter origin from activation-factor sufficiency.
- Do not privilege monitoring-language tokens or one atomic content region.
- Direct the next prospective experiment to upstream layer/component mediation of
  named-concept and trigger/other K/V states, using exact Day 60 KV recovery as the
  endpoint.
