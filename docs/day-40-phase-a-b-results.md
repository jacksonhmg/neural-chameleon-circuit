# Post–Gate 1 Phase A–B Results

Phase A–B passed its frozen implementation and scientific continue gates. This is existing-data mechanism-development evidence, not fresh confirmation, and the sealed original complete-writer Gate 1 remains failed.

## Complete execution

The final implementation audit passed every row-count, uniqueness, identity, runtime-provenance, precursor/random-control, replay, preflight, and artifact-preservation check. The effect matrix contains the exact 444,224 frozen rows: 41,568 absolute-contribution, 221,696 matched-random, 68,192 frontier, and 112,768 attention-operation rows. A second reducer run reproduced all six Day 40 JSON artifacts byte-for-byte.

The attention population required two prospective execution-only corrections after discovery. V8 passed its 256-row replay but exhausted the bounded allocator after 57 complete evaluation batches. V9 reduced child lifetime to 32 batches, passed exact two-checkpoint preflight, replayed all 1,824 V8 evaluation rows at zero tolerance, and completed both 738-batch checkpoint populations. No intervention, row, selection, metric, or gate changed.

## Mechanism signature

The selected account is acquired direct monitor writing/reconfiguration by K12:

- natural monitor-evasion gaps were present in all nine held-out positive concepts;
- natural K12 replacement recovered 0.797 of the complete standardized probe vector (one-sided 95% lower bound 0.742);
- its direct path recovered 0.529 of that vector and accounted for 0.687 of the total replacement effect;
- the selected layer-11 F3 frontier recovered 0.918 of the downstream remainder, versus 0.268 for the matched nonselected frontier;
- the exact precursor effect was 0.027 of Chameleon's effect, with a one-sided 95% upper bound of 0.056;
- matched random operations were 0.100 of the selected effect, with a one-sided 95% upper bound of 0.112; and
- the total replacement direction was positive in all nine held-out concepts.

The frozen negative-writing, omission, downstream-amplification, and hybrid gates did not pass. `concept_span_ov` was the discovery-selected attention-source diagnostic, but its held-out complete-vector recovery was only 0.110; it is not promoted to the central operation. The Phase C object is therefore the full selected-head K12 direct-reconfiguration effect, with the attention result retained as source-localization evidence only.

## Consequence

The scientific continue gate passed. Under Decision 0033, this permits drafting and, after a separate committed machine-readable contract, locally executing Phase C semantic conditioning. It does not permit a fresh-confirmation claim, the target title, or dissemination.

Primary artifacts are in [`results/day-40`](../results/day-40), with the complete audit in [`phase-a-b-audit.json`](../results/day-40/phase-a-b-audit.json) and the mechanical gate in [`mechanism-signature-summary.json`](../results/day-40/mechanism-signature-summary.json).
