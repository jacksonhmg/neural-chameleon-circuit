# Day 48 Proximal Upstream-Interface Result

## Result

The layer-9-input response-token residual is **not** the compact causal controller that configures K12. The frozen implementation gate passed, but the prospective scientific gate failed decisively in both directions.

| Direction | Median concept K12 recovery | Strongest control | Advantage | Required recovery / advantage |
|---|---:|---:|---:|---:|
| Correct-trigger response state → normal | 0.0475 | 0.0146 | 0.0328 | 0.50 / 0.25 |
| Normal response state → correct trigger | 0.0232 | 0.0150 | 0.00825 | 0.50 / 0.25 |

Sufficiency had positive K12 direction in all 13 concepts, but necessity did so in only 8. Neither direction approached the magnitude or control-advantage threshold. A single concept accounted for `0.375` of the sufficiency aligned numerator and `0.454` of the necessity numerator, failing the frozen distributed-concept clause.

The own-probe direction was correct in all 13 concepts in both directions, while median complete monitor-vector recovery was `0.1424` for sufficiency and `0.1215` for necessity. This adverse combination shows why monitor movement alone is insufficient: the response-state transplant can perturb the monitor in the expected direction without recreating the natural K12 computation.

## Fixed negative comparator

Repeating the Phase C different-trigger-to-correct named-concept-span substitution recovered median K12 trajectory `0.4538` and mean monitor-vector effect `0.4419`. This is materially larger than the response-row effect, but it does not overturn Phase C: the named span previously failed trigger specificity because irrelevant triggers were equally effective. It remains a non-specific negative comparator, not a promoted controller.

## Implementation audit

The corrected execution covered 26 examples, 13 concepts, 338 state rows, and 234 example-level metric rows. It completed in `9.11` seconds on one H100 PCIe and allocated `18.71` GB at peak.

- exact K12 identity error: `0.0`;
- maximum monitor-margin identity error: `3.81e-6`;
- all response IDs, masks, tensor counts, random-control invariants, finiteness, and hook cleanup passed; and
- two complete reductions were byte-identical.

An initial execution was correctly rejected before interpretation because independently batched natural and four-job intervention captures differed beyond the frozen K12 identity tolerance. Repeating the unpatched natural endpoints at the identical CUDA batch shape removed that numerical comparison error without changing the experiment.

## Disposition

Apply the frozen failure consequence: do not run a broad residual scan or backward localization. The only eligible next branch is the prospectively selected prompt-memory test at the K12 attention interfaces.

All evidence remains previously inspected development evidence. No fresh-confirmation or title upgrade follows.
