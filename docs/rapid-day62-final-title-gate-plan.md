# Day 62+: Final End-to-End Title-Gate Program

> **Historical stopped plan (2026-08-13):** This program stopped correctly when no
> unseen-concept pair qualified. Its conditional title wording is not the final project
> disposition. See [the qualification result](day-62-final-title-gate-qualification-results.md),
> [`PROJECT_STATUS.md`](../PROJECT_STATUS.md), and [Day 70](day-70-endpoint-correction-results.md).

## Objective

Timebox parameter acquisition localization, then test the complete causal chain on
concepts and examples absent from Neural Chameleon fine-tuning and all prior project
analyses. The target title remains conditional until every predeclared clause passes.

## Evidence separation

Six new concepts form three ordered candidate pairs: academic/casual,
formal/helpful, and angry/toxic. Exact-precursor activations from the released
training partition train six new affine probes. A disjoint validation role gates
probe quality. A separate Chameleon qualification role selects the earliest two
qualifying pairs and fixes K/V, K12, and complete-probe-vector predictions. A
negative-only calibration role fixes 1% and 5% FPR thresholds. The causal and
negative final roles remain locked until those artifacts are committed in a second
contract.

No final example can select a probe, pair, prototype, intervention, threshold, or
gate. No failed concept pair is replaced outside the frozen order.

## Timeboxed hybridization

On the opened Day 60 panel, replace Chameleon parameters with exact-precursor values
for four coarse objects only: blocks 0--8, selected K12 K/V rows, their complementary
layer-9--12 attention slices, and the union. Score old probe, K12, and pre-RoPE K/V
effects. Stop after this pass regardless of outcome. Hybridization is developmental
localization and cannot alter the final causal operator.

## Integrated final intervention

For each admitted A/B pair and both directions, use exact shared teacher-forced
responses and compare natural A and B triggers. Patch source-side K/V into the
opposite semantic context; clamp K12 to the opposite natural state; and restore the
original natural K12 state while leaving the semantic substitution and all 52
parallel tail heads active. Include a third-concept K/V control and a matched
orthogonal K12 control. Repeat the identical matrix in the exact precursor.

Primary predictions are frozen from qualification in three coordinates:
informative-source pre-RoPE K/V, raw K12 output, and the complete selected-new-probe
margin vector. Operational outcomes use calibration-only thresholds and report raw
margins, realized FPR, and TPR at nominal 1% and 5% FPR.

## Disposition

The full title is earned only if acquisition, operation, semantic conditioning,
necessity/sufficiency, endogenous propagation, restoration, and operational failure
all pass. The gate is conjunctive; a strong result in one coordinate cannot offset
a failed link.
