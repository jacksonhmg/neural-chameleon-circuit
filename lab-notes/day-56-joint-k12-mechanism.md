# Day 56: Joint-K12 Mathematical Mechanism

## 2026-08-12 — Prospective freeze

Decision 0088, the research plan, and the machine-readable contract freeze the complete 12-head causal object; exact six-region attention algebra; routing/content Shapley split; five joint candidate components; reciprocal installation, removal, Haar, and residual-closure metrics; dose/Jacobian/fixed-RMSNorm geometry classification; implementation tolerances; and stop rules before new outcomes.

The program reuses the unchanged Day 54 development sandbox. It does not search individual heads/layers, add candidate combinations after outcomes, or access fresh evidence.

## 2026-08-12 — Outcome-blind implementation and preflight

The exact attention-moment algebra, routing/content Shapley split, six-region interventions, joint Jacobian, fixed-RMSNorm counterfactual, resumable 13-concept runner, and deterministic reducer were committed before candidate outcomes. All 132 then 133 local tests passed.

The first preflight detected that an enclosing fixed-normalization hook was conservatively counted as a leaked intervention hook. No candidate outcome was generated and no preflight artifact was written. The control was changed to a reversible forward substitution, preserving the exact frozen intervention. The repaired preflight passed with zero duplicate-identity error, fixed-normalization identity error at most `0.0122`, attention reconstruction error at most `0.0157`, Shapley closure below `9.54e-7`, exact response tensors, finite values, and zero leaked hooks.

## 2026-08-12 — Invalid Jacobian batch-geometry attempt

The first complete matrix executed, but the reducer rejected it before scientific acceptance. The batch-2 Jacobian identity differed from the frozen batch-60 causal endpoint by `0.05569`, above the unchanged `0.05` implementation gate. The entire attempt was moved to a hash-indexed quarantine. No candidate, threshold, or scientific rule was changed.

The Jacobian was mechanically rerun with 30 repeated batch slots, matching the already-frozen causal matrix geometry. The repair was committed and the candidate-blind preflight rerun before the replacement matrix.

## 2026-08-12 — Valid result

The valid 13-concept run completed all 1,976 state rows and passed every audit. Jacobian identity error fell to `3.81e-6`; attention reconstruction, exact Shapley closure, Haar invariants, probe order, finiteness, shard uniqueness, duplicate reductions, and hook cleanup all passed.

Attended value content dominates routing. Content hybrids recover `0.782/0.866` of the complete-probe effect while routing hybrids recover only `0.100/0.206`. The monitoring-prefix component is the strongest causal approximation: installation `0.861/0.871`, removal `0.888/0.870`, matched-Haar advantage `0.741/0.741`, and remaining effect norm `0.165/0.175`. It passes four of five closure clauses but misses reverse donor identity at `8/13`; no compact operator is selected.

The named-concept and alternative-trigger regions carry the effect. Monitoring language, original prompt, template, and response history are small. Fixed RMSNorm denominators preserve or increase the effect (`1.058/1.158` recovery), rejecting simple normalization mediation. Dose curves and the high-cosine but low-magnitude target Jacobian establish the frozen `mixed_or_nonlinear_state_dependent` downstream classification.

All 35 valid manifest entries totaling 1,573,498,307 bytes were recovered and hash-verified. Independent local reduction matched every structure and label and differed by at most `2.38e-7` across 18,192 numeric cells. Decision 0089 applies the frozen disposition.

The exact project-owned H100 was terminated and confirmed absent from authenticated inventory. Maximum observed lifetime was 1,383 seconds, corresponding to at most approximately `$1.65` at list rate. The unrelated account instances were not modified.
