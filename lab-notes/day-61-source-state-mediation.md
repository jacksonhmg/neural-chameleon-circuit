# Day 61: Upstream Source-State Mediation

## 2026-08-12 — Prospective freeze

The Day 61 contract froze 37 candidates: residual inputs through layer 12 and whole
normalized attention/MLP outputs through layer 11. Every aligned donor transplant
at named-concept plus trigger/other source positions had a deterministic
signed-permutation control preserving hidden-delta row norms and temporal Gram
geometry. Endpoints were the exact Day 60 KV-caused monitor, raw K12, and aligned
pre-RoPE K/V states, globally and at each K12 layer. Both directions, gates,
selection rules, stop rules, and the `$100` ceiling were fixed in commit `fcbf8de`.

## Preflight and execution

The first dedicated H100 failed to become SSH-reachable and produced no candidate
outcomes. A replacement H100 passed every candidate-blind check with exactly zero
same-state error. It executed all 11 singleton concept shards: 44 examples, 6,688
state rows, and 3,256 passing control-geometry audits. Peak allocated CUDA memory
was 18,483,412,480 bytes.

## Mechanical result

No compact attention/MLP branch qualified. Residual boundaries 0--9 qualified in
both directions, producing the frozen `residual_state_only` classification and
earliest-rule selection `resid_pre.layer_00`. Pre-layer-9 was the final boundary
eligible to influence all selected heads at layers 9--12; later boundaries incurred
the expected zero recovery for already-computed K12 layers.

The donor early residual state recovered K/V and raw K12 near one but overshot the
Day 60 KV-only monitor effect. This indicates additional causal routes in the full
residual state and prevents interpreting the transplant as a pure K12 code. The
strongest isolated writer was MLP layer 8, with at most 0.258 monitor, 0.228 K12, and
0.176 K/V recovery across the two directions, well short of qualification.

## Recovery and closeout

All 22 files were recovered and hash-verified. Independent local reduction exactly
matched every gate, selection, and classification; its maximum numeric discrepancy
was `8.88e-16`. Both dedicated instance IDs were confirmed absent from inventory,
and the unrelated account instance was untouched. A deliberately conservative cost
bound is `$2.59`.
